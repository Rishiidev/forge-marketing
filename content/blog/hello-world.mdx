---
title: "Hello, world"
description: "An example post proving the MDX content pipeline works — not real Forge blog content."
date: "2026-01-01"
---

This is an example post. It exists to prove `content/blog/*.mdx` files are
read, parsed, and rendered correctly through `lib/content.ts` and
`next-mdx-remote`.

Replace or delete this file once real blog content exists. Nothing in it
should be treated as real Forge copy.
