# Forge — Business Rules

> **PLACEHOLDER DOCUMENT.** This file intentionally contains no invented
> business information. It exists as a structural placeholder so the
> rebuild has a single, named location to record pricing, offers, and
> funnel rules once they are confirmed.
>
> The current codebase (see `docs/legacy-readme.md` and the baseline
> commit) contains **more than one candidate pricing structure** across
> different pages (e.g. `index.html` vs `5000-setup.html` vs
> `operator.html`), and they do not agree with each other. Do not treat
> any single page as authoritative until this document is filled in by
> a human decision-maker.

## Canonical funnel

`[PLACEHOLDER — which funnel is canonical: the index.html tier structure,
the 5000-setup.html ladder, or something else? Needs an explicit decision.]`

## Pricing

`[PLACEHOLDER — confirmed prices for each tier/offer, once the canonical
funnel above is decided.]`

## Offers / templates

`[PLACEHOLDER — which industry templates are live, which are planned.]`

## Capacity / caps

`[PLACEHOLDER — the real monthly launch cap, Operator plan client cap,
and how they should be sourced (single config value, CMS, etc.) in the
rebuild.]`

## Contact channels

`[PLACEHOLDER — confirmed support email domain and WhatsApp number to use
site-wide. The baseline code ships with placeholder/inconsistent values
that must not be treated as real.]`
