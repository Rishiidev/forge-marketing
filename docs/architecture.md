# Forge Marketing — Architecture

> **PLACEHOLDER DOCUMENT.** No architecture decisions have been made yet
> beyond establishing this baseline repository. This file exists as a
> structural placeholder for the target architecture once it is chosen.

## Current (baseline) architecture

`[PLACEHOLDER — summarize the baseline architecture: static HTML pages
with inline CSS/JS, shared `forge-wa-submit.js` form handler, Vercel
serverless functions in `api/`. See `docs/legacy-readme.md` for what the
original authors documented.]`

## Target architecture

`[PLACEHOLDER — framework/tooling choice for the componentized rebuild,
component inventory, design-token strategy, build/deploy pipeline.]`

## Constraints carried forward

`[PLACEHOLDER — anything from the baseline that must be preserved
functionally: the capacity-cap mechanism, the three-leg lead capture
(email/DB/GitHub backup) in `api/submit.js`, the WhatsApp-first form
submission pattern, etc.]`
